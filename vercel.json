{
    "version": 2,
    "functions": { "api": { "runtime": "python3.10", "memory": 512 } },
    "routes": [
        { "src": "/predict_api", "dest": "/api/predict_api" },
        { "src": "/predict", "dest": "/api/predict" },
        { "src": "/(.*)", "dest": "/$1" }
    ]
}
